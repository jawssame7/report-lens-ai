# ReportLensAI 要件定義

## 1. 背景・目的

- PDF レポートの定期的な分析作業を自動化し、洞察抽出を効率化する
- 多様な形式のファイル(PDF、Excel、Word、画像等)の一括アップロード対応
- Gemini が自動で:
- 数値データを抽出・比較
- 前年比/前月比の増減分析
- 異常値や注目すべきポイントの検出
- React + TypeScript でインタラクティブなダッシュボード表示

## 2. ステークホルダー

- プロダクトオーナー: Hirofumi(開発者兼初期ユーザー)
- 利用部門/ユーザー:
  - 一次: Hirofumi 自身
  - 二次(将来): 経営層、財務担当者、マネージャー層
- 開発チーム: Hirofumi(フルスタック開発)

## 3. スコープ

### 3.1 対象

- 多様なファイル形式のレポート
  - PDF(テキスト埋め込み型 + スキャン画像型)
  - Excel(.xlsx, .xls)
  - Word(.docx, .doc)
  - CSV, TSV
  - PowerPoint(.pptx, .ppt)
  - 画像ファイル(.jpg, .png) ※表やグラフのスクリーンショット等
- 数値データの抽出と基本的な統計分析
- 複数ファイル間の比較分析(前月比、前年比等)
- 分析結果のビジュアル表示(グラフ、表、サマリー)
- 分析結果のエクスポート(CSV、PDF)

### 3.2 非対象

- リアルタイムデータ連携(ERP や CRM 等との直接統合)
- 複雑な財務予測や高度な機械学習モデル
- マルチユーザー/権限管理機能(初期は個人利用想定)
- 動画・音声ファイルの分析

## 4. 機能要件

1. **ファイルアップロード機能**

   - ドラッグ&ドロップまたはファイル選択で複数ファイルアップロード
   - 対応形式: PDF, Excel, Word, CSV, PowerPoint, 画像(JPG, PNG)
   - OCR 機能: スキャン画像 PDF や画像ファイルからのテキスト抽出
   - ファイル形式の自動判定
   - 最大 10 ファイル、合計 100MB
   - アップロード進捗表示
   - ファイル名、サイズ、形式、アップロード日時の表示

2. **データ抽出・分析機能**

   - Gemini API による各種ファイル形式からの数値データ自動抽出
   - 売上、利益、前年比等の主要指標の認識
   - 表やグラフからのデータ抽出
   - 複数ファイル間での同一指標の比較
   - Gemini Vision API による画像・スキャン PDF の OCR 処理

3. **インサイト生成機能**

   - 増減率の計算と異常値検出
   - 傾向分析(上昇/下降トレンド)
   - 注目ポイントの自動サマリー生成
   - 自然言語での分析結果説明

4. **ダッシュボード表示機能**

   - 主要指標のカード表示
   - 時系列グラフ(折れ線、棒グラフ)
   - 比較表(前月比、前年比)
   - AI による分析コメント表示

5. **エクスポート機能**

   - 分析結果の CSV ダウンロード
   - サマリーレポートの PDF 出力

6. **履歴管理機能**
   - 過去の分析履歴の保存(最新 20 件)
   - 過去分析の再表示

## 5. 非機能要件

- パフォーマンス:
  - ファイルアップロード: 10MB 以内のファイルは 5 秒以内
  - Gemini API 分析: 1 ファイルあたり 20 秒以内(OCR 含む)
  - ダッシュボード表示: 初期表示 3 秒以内
- セキュリティ:
  - Gemini API キーは環境変数で管理(Lambda)
  - アップロードファイルは一時保存のみ(分析後削除)
  - HTTPS 通信のみ
  - CORS 設定で自ドメインのみ許可
- 可用性:
  - Lambda + API Gateway で 99%以上の稼働率
  - CloudFront + S3 で静的コンテンツの高可用性確保
- 運用・保守:
  - CloudWatch Logs でエラー監視
  - Lambda 関数のバージョン管理
  - 月次コスト監視(目標: 月 5,000 円以内)

## 6. 入出力

- 入力:
  - 各種ファイル(PDF, Excel, Word, CSV, PowerPoint, 画像)
  - 最大 10 ファイル/回、合計 100MB
  - 分析対象期間の指定(オプション)
  - 比較基準の選択(前月比/前年比/カスタム)
- 出力:
  - 抽出データ一覧(JSON/CSV)
  - 数値比較結果(増減率、差分)
  - AI による分析サマリー(テキスト)
  - グラフ画像(PNG)
  - 総合レポート(PDF)

## 7. 成功基準

- 技術的成功:
  - 各種ファイルから主要指標を 90%以上の精度で抽出できる
  - 1 ファイルあたり 20 秒以内で分析完了
  - エラー率 5%未満
- ビジネス的成功:
  - 手動分析時間を 70%削減(例: 1 時間 →20 分)
  - 見落としていた異常値を月 3 件以上検出
  - 3 ヶ月継続使用で業務効率化を実感

## 8. 制約条件

- 技術制約:
  - Gemini API 利用制限(無料枠: 60 リクエスト/分)
  - Gemini Vision API 利用(画像・スキャン PDF 用)
  - Lambda 実行時間制限(最大 15 分)
  - API Gateway ペイロード制限(10MB)
  - 各種ファイル解析ライブラリ:
    - PDF: pdf-parse または pdf.js
    - Excel: xlsx, exceljs
    - Word: mammoth
    - 画像 OCR: Gemini Vision API
    - PowerPoint: pptx または officegen
- 予算制約:
  - 月間運用コスト上限: 5,000 円
  - 初期開発期間: 40 時間以内
- リソース制約:
  - 開発者 1 名(Hirofumi)
  - 開発時間: 平日夜間・週末のみ

## 9. リスクと対策

- **リスク 1: Gemini API の抽出精度が低い**
  - 対策: プロンプトエンジニアリングで精度向上、テストデータで検証
  - 代替案: 複数モデル(Claude、GPT)の併用検討
- **リスク 2: ファイル形式ごとの処理複雑度**
  - 対策: フェーズ 1 では PDF と Excel のみ対応、段階的に拡大
  - 優先順位: PDF → Excel → Word → 画像 → CSV → PowerPoint
- **リスク 3: API 利用コストの増加**
  - 対策: 使用量監視とアラート設定、キャッシュ戦略の導入
- **リスク 4: Lambda 処理時間超過**
  - 対策: 大きいファイルは分割処理、非同期処理の導入
- **リスク 5: OCR 精度の問題**
  - 対策: Gemini Vision API の精度を事前検証、低品質画像は警告表示
